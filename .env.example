VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Netlify / Vercel: add the same VITE_* keys under site/project → Environment variables
# (Production + Preview). Without them, the app may build but Supabase will not work at runtime.
