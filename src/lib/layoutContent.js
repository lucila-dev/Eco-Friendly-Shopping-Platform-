/** Readable line length; slightly narrower than before for a tighter, calmer layout on wide screens. */
export const layoutContentWidthClass = 'max-w-[min(100%,64rem)] mx-auto w-full'

export const layoutContentPadClass = 'px-3 sm:px-5 lg:px-6 xl:px-8'
