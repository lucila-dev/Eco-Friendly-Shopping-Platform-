/** Wider than max-w-7xl on large screens so copy uses horizontal space; still capped for very wide monitors. */
export const layoutContentWidthClass = 'max-w-[min(100%,92rem)] mx-auto w-full'

export const layoutContentPadClass = 'px-4 sm:px-6 lg:px-8 xl:px-10'
